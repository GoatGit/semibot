# PRD: Skills 和 MCP 管理页面

## 概述

| 字段 | 值 |
|------|-----|
| **功能名称** | Skills 和 MCP 管理页面 |
| **版本** | 1.1 |
| **优先级** | High |
| **关联任务** | [TASK](../TASKS/skills-mcp-management-pages.md) |

## 1. 背景与目标

### 1.1 背景
Semibot 作为 AI Agent 平台，需要支持：
- **Skills（技能）**: Agent 可调用的能力模块，兼容 Anthropic Claude Skills 协议
- **MCP 服务器**: 外部工具和数据源连接，完全兼容 Anthropic MCP 协议规范

当前缺少这两个核心功能的管理界面，需要支持第三方 Skills 和 MCP 的添加、删除和管理。

### 1.2 目标
- 提供直观的 Skills 管理界面，支持第三方技能导入
- 提供 MCP 服务器配置和监控界面，兼容 Anthropic MCP 协议
- 与现有 Agents、Settings 页面风格保持一致
- 支持完整的 CRUD 操作

---

## 2. Anthropic 协议兼容性

### 2.1 Skills 协议兼容

遵循 Claude Code Skills 规范：

```typescript
// Anthropic Skills 格式
interface AnthropicSkill {
  name: string                    // 技能名称
  description: string             // 技能描述
  version?: string                // 版本号
  author?: string                 // 作者
  triggers?: string[]             // 触发关键词
  instructions: string            // 技能指令
  tools?: string[]                // 依赖工具
}
```

### 2.2 MCP 协议兼容

完全兼容 Anthropic MCP 协议规范：

```typescript
// Anthropic MCP Server 配置格式
interface AnthropicMCPConfig {
  mcpServers: {
    [name: string]: {
      // stdio 模式
      command?: string
      args?: string[]
      env?: Record<string, string>
      cwd?: string
      // SSE/HTTP 模式
      url?: string
      headers?: Record<string, string>
    }
  }
}
```

---

## 3. 功能需求

### 3.1 Skills 管理页面 (`/skills`)

#### 3.1.1 页面结构
```
┌─────────────────────────────────────────────────────────────────┐
│ Skills                                    [+ 添加] [↓ 导入]     │
│ 管理您的 Agent 技能，共 X 个                                     │
├─────────────────────────────────────────────────────────────────┤
│ [搜索框]     [全部] [已启用] [已禁用] [内置] [自定义] [第三方]    │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐                │
│ │ 🔍 Web Search│ │ 💻 Code Exec │ │ 📁 File Mgr  │                │
│ │ 内置 | v1.0  │ │ 内置 | v1.0  │ │ 内置 | v1.0  │                │
│ │ 描述...     │ │ 描述...     │ │ 描述...     │                │
│ │ [开关 ✓]    │ │ [开关 ✓]    │ │ [开关 ✓]    │                │
│ └─────────────┘ └─────────────┘ └─────────────┘                │
│ ┌─────────────┐ ┌─────────────┐                                │
│ │ 🐙 GitHub   │ │ 📝 Notion    │                                │
│ │ 第三方|v2.1 │ │ 第三方|v1.5 │                                │
│ │ 描述...     │ │ 描述...     │                                │
│ │ [开关] [删除]│ │ [开关] [删除]│                                │
│ └─────────────┘ └─────────────┘                                │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.1.2 Skill 卡片内容
- 图标 + 名称 + 版本
- 来源标签：`内置` / `自定义` / `社区` / `第三方`
- 描述（2行截断）
- 启用/禁用开关
- 更多菜单：查看详情、配置、删除（仅非内置）

#### 3.1.3 添加/导入 Skill 弹窗

```
┌─────────────────────────────────────────────────┐
│ 添加技能                                   [×]  │
├─────────────────────────────────────────────────┤
│ 导入方式:                                       │
│ ○ 本地文件 (.md / .json)                        │
│ ○ URL 导入                                      │
│ ● GitHub 仓库                                   │
│                                                 │
│ GitHub 仓库地址:                                │
│ ┌─────────────────────────────────────────────┐ │
│ │ https://github.com/user/skill-repo          │ │
│ └─────────────────────────────────────────────┘ │
│                                                 │
│ 分支/标签 (可选):                               │
│ ┌─────────────────────────────────────────────┐ │
│ │ main                                        │ │
│ └─────────────────────────────────────────────┘ │
│                                                 │
│                    [取消]  [验证并添加]          │
└─────────────────────────────────────────────────┘
```

#### 3.1.4 Skill 数据结构

```typescript
interface Skill {
  id: string
  name: string
  description: string
  icon: string                           // 图标名称或 URL
  version: string
  author?: string
  category: 'builtin' | 'custom' | 'community' | 'third-party'
  source: SkillSource
  enabled: boolean
  triggers?: string[]                    // 触发关键词
  instructions: string                   // 技能指令内容
  tools?: string[]                       // 依赖的工具
  config?: Record<string, unknown>       // 配置参数
  permissions?: string[]                 // 所需权限
  createdAt: string
  updatedAt: string
}

type SkillSource =
  | { type: 'builtin' }                                    // 内置
  | { type: 'local'; path: string }                        // 本地文件
  | { type: 'url'; url: string }                           // URL 导入
  | { type: 'github'; repo: string; ref?: string }         // GitHub 仓库
  | { type: 'registry'; name: string; version: string }    // 官方仓库
```

---

### 3.2 MCP 管理页面 (`/mcp`)

#### 3.2.1 页面结构
```
┌─────────────────────────────────────────────────────────────────┐
│ MCP 服务器                                        [+ 添加服务器] │
│ 管理外部工具和数据源连接 (兼容 Anthropic MCP 协议)               │
├─────────────────────────────────────────────────────────────────┤
│ [全部] [已连接] [连接中] [已断开] [错误]                         │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🟢 Filesystem MCP                              内置 | stdio │ │
│ │    提供文件系统读写能力                                      │ │
│ │    工具: 5 个 | 资源: 0 个                                   │ │
│ │    最后连接: 刚刚                        [配置] [断开] [测试] │ │
│ └─────────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🟢 GitHub MCP                              第三方 | stdio   │ │
│ │    npx -y @anthropic/github-mcp                             │ │
│ │    工具: 12 个 | 资源: 3 个                                  │ │
│ │    最后连接: 5 分钟前              [配置] [断开] [测试] [删除] │ │
│ └─────────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🔴 Custom API Server                        自定义 | sse    │ │
│ │    https://api.mycompany.com/mcp                            │ │
│ │    错误: Connection refused                                  │ │
│ │                                       [配置] [重试] [删除]   │ │
│ └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.2.2 MCP 服务器卡片内容
- 连接状态指示灯（🟢绿/🟡黄/🔴红）
- 服务器名称 + 来源标签 + 传输类型
- 端点信息（命令或URL）
- 工具/资源/提示数量
- 最后连接时间或错误信息
- 操作按钮：配置、连接/断开、测试、删除（仅非内置）

#### 3.2.3 添加 MCP 服务器弹窗

```
┌─────────────────────────────────────────────────────────────────┐
│ 添加 MCP 服务器 (Anthropic 协议)                           [×]  │
├─────────────────────────────────────────────────────────────────┤
│ 服务器名称 *                                                    │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ My MCP Server                                               │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ 传输类型 *                                                      │
│ ● stdio (本地进程)                                              │
│ ○ SSE (远程服务)                                                │
│ ○ Streamable HTTP (流式HTTP)                                    │
│                                                                 │
│ ═══════════════════════════════════════════════════════════════ │
│ stdio 配置:                                                     │
│                                                                 │
│ 命令 *                                                          │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ npx                                                         │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ 参数 (每行一个)                                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ -y                                                          │ │
│ │ @anthropic/github-mcp                                       │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ 环境变量 (KEY=VALUE 格式)                                       │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ GITHUB_TOKEN=ghp_xxxxxxxxxxxx                               │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│ 工作目录 (可选)                                                 │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ /Users/user/project                                         │ │
│ └─────────────────────────────────────────────────────────────┘ │
│                                                                 │
│                         [取消]  [测试连接]  [保存]              │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.2.4 MCP 服务器数据结构

```typescript
interface MCPServer {
  id: string
  name: string
  description?: string
  category: 'builtin' | 'official' | 'third-party' | 'custom'
  transport: MCPTransport
  status: 'connected' | 'connecting' | 'disconnected' | 'error'
  tools: MCPTool[]
  resources?: MCPResource[]
  prompts?: MCPPrompt[]
  lastConnected?: string
  error?: string
  config?: Record<string, unknown>
  createdAt: string
  updatedAt: string
}

// 兼容 Anthropic MCP 协议的传输配置
type MCPTransport =
  | {
      type: 'stdio'
      command: string
      args?: string[]
      env?: Record<string, string>
      cwd?: string
    }
  | {
      type: 'sse'
      url: string
      headers?: Record<string, string>
    }
  | {
      type: 'streamable-http'
      url: string
      headers?: Record<string, string>
    }

interface MCPTool {
  name: string
  description: string
  inputSchema: JSONSchema
}

interface MCPResource {
  uri: string
  name: string
  mimeType?: string
  description?: string
}

interface MCPPrompt {
  name: string
  description?: string
  arguments?: { name: string; required?: boolean }[]
}
```

#### 3.2.5 工具详情弹窗

```
┌─────────────────────────────────────────────────────────────────┐
│ GitHub MCP - 工具列表                                      [×]  │
├─────────────────────────────────────────────────────────────────┤
│ 共 12 个工具可用                                                │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 📋 list_repos                                               │ │
│ │    列出用户或组织的仓库                                      │ │
│ │    参数: owner (string), page (number)                      │ │
│ └─────────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 📂 get_file                                                 │ │
│ │    获取仓库中的文件内容                                      │ │
│ │    参数: owner, repo, path, ref                             │ │
│ └─────────────────────────────────────────────────────────────┘ │
│ ...                                                             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. 导航集成

### 4.1 NavBar 更新

在 `navItems` 数组中添加：

```typescript
import { Sparkles, Server } from 'lucide-react'

const navItems: NavItem[] = [
  { icon: <Home size={20} />, label: '首页', href: '/' },
  { icon: <Bot size={20} />, label: 'Agents', href: '/agents' },
  { icon: <Sparkles size={20} />, label: 'Skills', href: '/skills' },  // 新增
  { icon: <Server size={20} />, label: 'MCP', href: '/mcp' },          // 新增
  { icon: <Settings size={20} />, label: '设置', href: '/settings' },
]
```

---

## 5. API 端点设计

### 5.1 Skills API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | /api/v1/skills | 获取技能列表 |
| GET | /api/v1/skills/:id | 获取技能详情 |
| POST | /api/v1/skills | 创建自定义技能 |
| POST | /api/v1/skills/import | 从 URL/GitHub 导入 |
| PUT | /api/v1/skills/:id | 更新技能配置 |
| DELETE | /api/v1/skills/:id | 删除技能 |
| POST | /api/v1/skills/:id/enable | 启用技能 |
| POST | /api/v1/skills/:id/disable | 禁用技能 |

### 5.2 MCP API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | /api/v1/mcp/servers | 获取服务器列表 |
| GET | /api/v1/mcp/servers/:id | 获取服务器详情 |
| POST | /api/v1/mcp/servers | 添加服务器 |
| PUT | /api/v1/mcp/servers/:id | 更新服务器配置 |
| DELETE | /api/v1/mcp/servers/:id | 删除服务器 |
| POST | /api/v1/mcp/servers/:id/connect | 连接服务器 |
| POST | /api/v1/mcp/servers/:id/disconnect | 断开连接 |
| POST | /api/v1/mcp/servers/:id/test | 测试连接 |
| GET | /api/v1/mcp/servers/:id/tools | 获取工具列表 |

---

## 6. 文件结构

```
apps/web/app/(dashboard)/
├── skills/
│   └── page.tsx           # Skills 列表页
├── mcp/
│   └── page.tsx           # MCP 服务器列表页

apps/web/components/
├── skills/
│   ├── SkillCard.tsx      # 技能卡片
│   ├── SkillList.tsx      # 技能列表
│   ├── AddSkillModal.tsx  # 添加技能弹窗
│   └── SkillDetailModal.tsx
├── mcp/
│   ├── MCPServerCard.tsx  # 服务器卡片
│   ├── MCPServerList.tsx  # 服务器列表
│   ├── AddMCPModal.tsx    # 添加服务器弹窗
│   └── MCPToolsModal.tsx  # 工具列表弹窗
```

---

## 7. Mock 数据

### 7.1 Skills Mock

```typescript
const mockSkills: Skill[] = [
  {
    id: 'skill-web-search',
    name: 'Web Search',
    description: '搜索互联网获取最新信息',
    icon: 'search',
    category: 'builtin',
    source: { type: 'builtin' },
    enabled: true,
    version: '1.0.0',
    instructions: '使用此技能搜索网页内容...',
    createdAt: '2026-01-01T00:00:00Z',
    updatedAt: '2026-01-01T00:00:00Z',
  },
  {
    id: 'skill-github',
    name: 'GitHub Integration',
    description: '与 GitHub 仓库交互，搜索代码、创建 Issue 等',
    icon: 'github',
    category: 'third-party',
    source: { type: 'github', repo: 'anthropics/github-skill', ref: 'main' },
    enabled: true,
    version: '2.1.0',
    author: 'Anthropic',
    instructions: '使用此技能与 GitHub 交互...',
    createdAt: '2026-02-01T00:00:00Z',
    updatedAt: '2026-02-01T00:00:00Z',
  },
]
```

### 7.2 MCP Servers Mock

```typescript
const mockMCPServers: MCPServer[] = [
  {
    id: 'mcp-filesystem',
    name: 'Filesystem',
    description: '提供文件系统读写能力',
    category: 'builtin',
    transport: { type: 'stdio', command: 'npx', args: ['-y', '@anthropic/filesystem-mcp'] },
    status: 'connected',
    tools: [
      { name: 'read_file', description: '读取文件内容', inputSchema: {} },
      { name: 'write_file', description: '写入文件内容', inputSchema: {} },
    ],
    lastConnected: '2026-02-05T14:30:00Z',
    createdAt: '2026-01-01T00:00:00Z',
    updatedAt: '2026-01-01T00:00:00Z',
  },
  {
    id: 'mcp-github',
    name: 'GitHub MCP',
    description: 'GitHub API 集成',
    category: 'third-party',
    transport: {
      type: 'stdio',
      command: 'npx',
      args: ['-y', '@anthropic/github-mcp'],
      env: { GITHUB_TOKEN: 'ghp_xxx' }
    },
    status: 'connected',
    tools: [
      { name: 'list_repos', description: '列出仓库', inputSchema: {} },
      { name: 'get_file', description: '获取文件', inputSchema: {} },
    ],
    lastConnected: '2026-02-05T14:25:00Z',
    createdAt: '2026-02-01T00:00:00Z',
    updatedAt: '2026-02-01T00:00:00Z',
  },
]
```

---

## 8. 实现步骤

### Phase 1: 基础页面
1. 创建 `/skills/page.tsx` - Skills 列表页
2. 创建 `/mcp/page.tsx` - MCP 列表页
3. 更新 `NavBar.tsx` 添加导航入口

### Phase 2: 交互功能
4. 实现搜索和筛选
5. 实现启用/禁用切换
6. 实现 MCP 连接状态显示

### Phase 3: 添加/导入功能
7. Skills 添加/导入弹窗
8. MCP 服务器添加弹窗
9. 连接测试功能

### Phase 4: 删除和配置
10. 技能删除功能（仅非内置）
11. MCP 服务器删除功能（仅非内置）
12. 配置编辑功能

---

## 9. 安全考虑

| 风险 | 缓解措施 |
|------|----------|
| 第三方技能恶意代码 | 技能执行沙箱隔离，权限审批机制 |
| MCP 服务器密钥泄露 | 环境变量加密存储，不在前端显示完整密钥 |
| stdio 命令注入 | 白名单命令校验，参数转义 |
| 未授权删除 | 内置资源不可删除，删除需确认 |

---

## 10. 验收标准

- [ ] Skills 页面正常渲染
- [ ] MCP 页面正常渲染
- [ ] 导航栏入口可点击跳转
- [ ] 搜索筛选功能正常
- [ ] 启用/禁用开关有反馈
- [ ] 第三方技能可添加/删除
- [ ] MCP 服务器可添加/删除
- [ ] 连接测试功能正常
- [ ] 页面响应式适配
- [ ] 代码通过 TypeScript 检查
