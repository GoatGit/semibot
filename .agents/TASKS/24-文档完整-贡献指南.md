# 任务：贡献指南

**优先级**: 🟡 P1 - 高优先级
**类型**: 文档完整
**预估工时**: 0.5 天
**影响范围**: 项目根目录

---

## 问题描述

项目缺少 `CONTRIBUTING.md` 贡献指南，导致：
1. 新贡献者不知道如何参与
2. 代码提交规范不统一
3. PR 流程不清晰

---

## 文档内容

### CONTRIBUTING.md

```markdown
# 贡献指南

感谢您有兴趣为 Semibot 做出贡献！本文档将帮助您了解如何参与项目开发。

## 行为准则

请在参与项目时遵守以下原则：
- 尊重所有贡献者
- 保持专业和友好的交流
- 接受建设性的批评
- 关注项目的最佳利益

## 如何贡献

### 报告问题

1. 在 [Issues](https://github.com/your-org/semibot/issues) 中搜索是否已有类似问题
2. 如果没有，创建新 Issue，包含：
   - 清晰的标题
   - 详细的问题描述
   - 复现步骤
   - 期望行为 vs 实际行为
   - 环境信息（系统、版本等）

### 提交代码

1. **Fork 仓库**
   ```bash
   git clone https://github.com/your-username/semibot.git
   cd semibot
   ```

2. **创建分支**
   ```bash
   git checkout -b feature/your-feature-name
   # 或
   git checkout -b fix/your-bug-fix
   ```

3. **安装依赖**
   ```bash
   pnpm install
   ```

4. **进行开发**
   - 遵循编码规范
   - 添加测试
   - 更新文档

5. **提交代码**
   ```bash
   git add .
   git commit -m "feat: add new feature"
   ```

6. **推送并创建 PR**
   ```bash
   git push origin feature/your-feature-name
   ```

## 开发环境设置

### 系统要求

- Node.js >= 18.0.0
- pnpm >= 8.0.0
- Python >= 3.11
- PostgreSQL >= 15
- Redis >= 7

### 安装步骤

1. 克隆仓库
   ```bash
   git clone https://github.com/your-org/semibot.git
   cd semibot
   ```

2. 安装 Node.js 依赖
   ```bash
   pnpm install
   ```

3. 安装 Python 依赖
   ```bash
   cd runtime
   python -m venv venv
   source venv/bin/activate  # Windows: venv\Scripts\activate
   pip install -r requirements.txt
   ```

4. 配置环境变量
   ```bash
   cp .env.example .env
   # 编辑 .env 文件
   ```

5. 启动开发服务器
   ```bash
   pnpm dev
   ```

## 编码规范

### 代码风格

- **TypeScript/JavaScript**: 使用 Prettier 格式化，ESLint 检查
- **Python**: 使用 Black 格式化，Ruff 检查

### 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 文件名 | kebab-case | `user-service.ts` |
| 类名 | PascalCase | `UserService` |
| 函数名 | camelCase | `getUserById` |
| 常量 | SCREAMING_SNAKE_CASE | `MAX_RETRY_COUNT` |
| 数据库字段 | snake_case | `created_at` |
| API 字段 | camelCase | `createdAt` |

### 提交消息规范

使用 [Conventional Commits](https://www.conventionalcommits.org/)：

```
<type>(<scope>): <description>

[optional body]

[optional footer(s)]
```

类型：
- `feat`: 新功能
- `fix`: 修复 bug
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `test`: 测试
- `chore`: 构建/工具

示例：
```
feat(api): add user authentication endpoint
fix(web): resolve login page rendering issue
docs: update API documentation
```

## 测试要求

### 编写测试

- 新功能必须包含测试
- Bug 修复应添加回归测试
- 目标覆盖率 >= 80%

### 运行测试

```bash
# 运行所有测试
pnpm test

# 运行特定模块测试
pnpm --filter @semibot/api test
pnpm --filter @semibot/web test

# Python 测试
cd runtime
pytest
```

## Pull Request 流程

### PR 标题

使用与提交消息相同的规范：
```
feat(api): add new authentication method
```

### PR 描述

使用模板填写：
- **变更摘要**: 简述变更内容
- **相关 Issue**: 关联的 Issue 编号
- **测试计划**: 如何测试这些变更
- **截图**: 如果涉及 UI 变更

### 审查流程

1. 自动化检查通过（CI/CD）
2. 至少一名维护者审查
3. 解决所有评论
4. 审查者批准后合并

### 合并策略

- 使用 "Squash and merge"
- 确保合并消息清晰

## 目录结构

```
semibot/
├── apps/
│   ├── api/          # 后端 API
│   └── web/          # 前端应用
├── packages/
│   ├── shared-types/ # 共享类型
│   └── ui/           # UI 组件库
├── runtime/          # Python Runtime
├── docs/             # 文档
└── .claude/          # 开发规范
```

## 需要帮助？

- 查看 [文档](./docs)
- 在 Issues 中提问
- 联系维护者

## 致谢

感谢所有贡献者！
```

---

## 修复清单

- [ ] 创建 `CONTRIBUTING.md`
- [ ] 添加 Issue 模板
- [ ] 添加 PR 模板
- [ ] 配置 CI 检查

---

## 完成标准

- [ ] 贡献指南完整
- [ ] 开发流程清晰
- [ ] 规范要求明确
- [ ] 代码审查通过

---

## 相关文档

- [编码规范](.claude/rules/coding-standards.md)
- [Git 工作流](.claude/rules/git-workflow.md)
