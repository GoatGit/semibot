# 任务：新增 Tooltip 组件，补全图标按钮可访问性

**优先级**: 🟢 P2
**类型**: 前端规范
**预估工时**: 2h
**影响范围**: 新增 1 个组件 + 修改 3 个页面

---

## 问题描述

agents、mcp 页面的图标按钮（编辑、删除、开关、同步等）仅有图标无文字，缺少 hover 提示。用户需要猜测按钮功能，可访问性不足。

---

## 修复方案

### 新建 `apps/web/components/ui/Tooltip.tsx`

```tsx
interface TooltipProps {
  content: string
  side?: 'top' | 'right' | 'bottom' | 'left'
  delay?: number  // 默认 300ms
  children: React.ReactNode
}
```

功能要求：
- hover 延迟显示（避免闪烁）
- 支持四个方向
- 自动避免溢出屏幕
- 使用设计系统颜色（`bg-bg-elevated`、`text-text-primary`）
- 小箭头指向触发元素

### 添加 Tooltip 的位置

1. `agents/page.tsx` — Power（启用/停用）、Settings（编辑）、Trash2（删除）按钮
2. `mcp/page.tsx` — Trash2（删除）按钮
3. `skill-definitions/page.tsx` — Pencil（编辑）、Trash2（删除）按钮

---

## 修复清单

- [ ] 创建 `apps/web/components/ui/Tooltip.tsx`
- [ ] 支持 4 个方向和延迟显示
- [ ] 为 agents 页面图标按钮添加 Tooltip
- [ ] 为 mcp 页面图标按钮添加 Tooltip
- [ ] 为 skill-definitions 页面图标按钮添加 Tooltip
- [ ] 暗色/亮色主题下验证

---

## 完成标准

- 所有图标按钮 hover 时显示操作提示
- Tooltip 在暗色/亮色主题下显示正常
- 不影响现有交互行为

---

## 相关文档

- [前端规范](../../.claude/rules/frontend.md)
- [PRD: UI 美化](../PRDS/ui-beautification-design-system.md)
