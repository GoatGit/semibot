# 任务：统一页面布局、空状态和加载状态

**优先级**: 🟢 P2
**类型**: 前端规范
**预估工时**: 2-3h
**影响范围**: 修改 1-2 个页面

---

## 问题描述

各 dashboard 页面的头部布局、空状态、加载状态风格不统一：

### 头部布局
- agents/mcp: `<header>` + `border-b border-border-subtle` + `px-6 py-4`
- skill-definitions: `<div>` + `space-y-6`，无边框分隔

### 空状态
- agents: 精心设计的 EmptyState 组件（图标 + 标题 + 描述 + 操作按钮）
- mcp: 简单的图标 + 文字 + 按钮
- skill-definitions: 仅一行 `text-gray-500` 文字

### 加载状态
- agents: `<Loader2>` 图标 spin
- mcp: `<Loader2>` 图标 spin + 文字
- skill-definitions: `border-b-2 border-blue-600` 圆形 spinner + 文字

---

## 修复方案

### 1. skill-definitions 页面头部对齐

改为与 agents/mcp 一致的结构：

```tsx
<header className="flex-shrink-0 border-b border-border-subtle px-6 py-4">
  <div className="flex items-center justify-between">
    <div>
      <h1 className="text-xl font-semibold text-text-primary">技能管理</h1>
      <p className="text-sm text-text-secondary mt-1">管理平台技能定义</p>
    </div>
    <div className="flex items-center gap-2">
      <Button variant="secondary" leftIcon={<RefreshCw size={16} />} onClick={loadDefinitions}>刷新</Button>
      <Button leftIcon={<Plus size={16} />}>创建技能</Button>
    </div>
  </div>
  <div className="mt-4 max-w-md">
    <Input placeholder="搜索技能..." leftIcon={<Search size={16} />} />
  </div>
</header>
```

### 2. 统一空状态

skill-definitions 空状态改为与 agents 一致的结构：图标 + 标题 + 描述 + 操作按钮。

### 3. 统一加载状态

所有页面使用 `<Loader2>` 图标 + `animate-spin` + 文字。

---

## 修复清单

- [ ] skill-definitions 页面头部改为 `header` + `border-b` 结构
- [ ] skill-definitions 空状态改为图标 + 标题 + 描述 + 按钮
- [ ] skill-definitions 加载状态改为 Loader2 图标
- [ ] 验证页面整体布局一致性

---

## 完成标准

- 所有 dashboard 页面头部结构一致
- 空状态和加载状态风格统一
- 暗色/亮色主题下显示正常

---

## 相关文档

- [前端规范](../../.claude/rules/frontend.md)
- [PRD: UI 美化](../PRDS/ui-beautification-design-system.md)
