# Semibot Design System

> 版本 1.0 | 2026-02-05 | 状态：草稿

## 1. 设计理念

### 1.1 品牌调性

**"精密智能工具"** — 专业、高效、值得信赖

Semibot 是面向开发者和企业的 Agent 编排平台，设计语言应传达：

| 关键词 | 体现 |
|--------|------|
| **精密** | 网格化布局、像素级对齐、信息密度适中 |
| **智能** | 流畅的状态反馈、有节奏的动效、清晰的进程可视化 |
| **专业** | 克制的装饰、功能导向、高对比度可读性 |
| **可信** | 一致的交互模式、可预测的行为、透明的执行过程 |

### 1.2 设计原则

```text
┌─────────────────────────────────────────────────────────────────┐
│                       设计原则金字塔                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│                         ┌─────────┐                             │
│                         │  愉悦   │  ← 超预期的细节体验          │
│                        ┌┴─────────┴┐                            │
│                        │   高效    │  ← 减少认知负担             │
│                       ┌┴───────────┴┐                           │
│                       │    清晰     │  ← 信息层次分明            │
│                      ┌┴─────────────┴┐                          │
│                      │     可用      │  ← 功能完整、无障碍       │
│                     └───────────────────┘                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**1. 清晰优先**
- 信息层次一目了然
- 状态反馈即时明确
- 操作路径简短直接

**2. 密度适中**
- 专业工具需要信息密度，但不应拥挤
- 留白引导视线，而非填满每一寸空间
- 复杂数据用渐进式披露

**3. 动效有意义**
- 每个动画都有目的（反馈、引导、过渡）
- 快速响应 > 华丽效果
- 状态变化要有视觉连续性

**4. 一致性**
- 相同功能相同外观
- 可预测的交互模式
- 跨设备体验统一

---

## 2. 色彩系统

### 2.1 设计理念

采用 **深色优先** 策略，符合开发者工具的使用场景（长时间使用、夜间工作）。

避免 AI 产品常见的紫色渐变，选择 **冷灰 + 青绿信号色** 的专业工具调性。

### 2.2 基础色板

```css
:root {
  /* ═══════════════════════════════════════════════════════════════
     Primary - 青绿色 (Teal/Cyan)
     代表智能、科技、可信赖
     ═══════════════════════════════════════════════════════════════ */
  --color-primary-50:  #ecfeff;
  --color-primary-100: #cffafe;
  --color-primary-200: #a5f3fc;
  --color-primary-300: #67e8f9;
  --color-primary-400: #22d3ee;
  --color-primary-500: #06b6d4;  /* 主色 */
  --color-primary-600: #0891b2;
  --color-primary-700: #0e7490;
  --color-primary-800: #155e75;
  --color-primary-900: #164e63;
  --color-primary-950: #083344;

  /* ═══════════════════════════════════════════════════════════════
     Neutral - 冷灰色
     深色主题基础，带有微妙的蓝色调
     ═══════════════════════════════════════════════════════════════ */
  --color-neutral-0:   #ffffff;
  --color-neutral-50:  #f8fafc;
  --color-neutral-100: #f1f5f9;
  --color-neutral-200: #e2e8f0;
  --color-neutral-300: #cbd5e1;
  --color-neutral-400: #94a3b8;
  --color-neutral-500: #64748b;
  --color-neutral-600: #475569;
  --color-neutral-700: #334155;
  --color-neutral-800: #1e293b;
  --color-neutral-900: #0f172a;
  --color-neutral-950: #020617;

  /* ═══════════════════════════════════════════════════════════════
     Semantic - 语义色
     ═══════════════════════════════════════════════════════════════ */

  /* Success - 绿色 */
  --color-success-50:  #ecfdf5;
  --color-success-100: #d1fae5;
  --color-success-500: #10b981;
  --color-success-600: #059669;
  --color-success-900: #064e3b;

  /* Warning - 琥珀色 */
  --color-warning-50:  #fffbeb;
  --color-warning-100: #fef3c7;
  --color-warning-500: #f59e0b;
  --color-warning-600: #d97706;
  --color-warning-900: #78350f;

  /* Error - 红色 */
  --color-error-50:  #fef2f2;
  --color-error-100: #fee2e2;
  --color-error-500: #ef4444;
  --color-error-600: #dc2626;
  --color-error-900: #7f1d1d;

  /* Info - 蓝色 */
  --color-info-50:  #eff6ff;
  --color-info-100: #dbeafe;
  --color-info-500: #3b82f6;
  --color-info-600: #2563eb;
  --color-info-900: #1e3a8a;
}
```

### 2.3 主题定义

#### 暗色主题 (默认)

```css
[data-theme="dark"] {
  /* 背景层级 */
  --bg-base:      var(--color-neutral-950);  /* #020617 最底层 */
  --bg-surface:   var(--color-neutral-900);  /* #0f172a 卡片/面板 */
  --bg-elevated:  var(--color-neutral-800);  /* #1e293b 悬浮/弹层 */
  --bg-overlay:   rgba(0, 0, 0, 0.6);         /* 遮罩层 */

  /* 文字层级 */
  --text-primary:   var(--color-neutral-50);   /* #f8fafc 主要文字 */
  --text-secondary: var(--color-neutral-400);  /* #94a3b8 次要文字 */
  --text-tertiary:  var(--color-neutral-500);  /* #64748b 辅助文字 */
  --text-disabled:  var(--color-neutral-600);  /* #475569 禁用文字 */
  --text-inverse:   var(--color-neutral-900);  /* #0f172a 反色文字 */

  /* 边框 */
  --border-default: var(--color-neutral-700);  /* #334155 默认边框 */
  --border-subtle:  var(--color-neutral-800);  /* #1e293b 细微边框 */
  --border-strong:  var(--color-neutral-600);  /* #475569 强调边框 */

  /* 交互状态 */
  --interactive-hover:  rgba(148, 163, 184, 0.1);  /* hover 背景 */
  --interactive-active: rgba(148, 163, 184, 0.2);  /* active 背景 */
  --interactive-focus:  var(--color-primary-500);   /* focus 环 */
}
```

#### 亮色主题

```css
[data-theme="light"] {
  /* 背景层级 */
  --bg-base:      var(--color-neutral-50);   /* #f8fafc */
  --bg-surface:   var(--color-neutral-0);    /* #ffffff */
  --bg-elevated:  var(--color-neutral-0);    /* #ffffff */
  --bg-overlay:   rgba(0, 0, 0, 0.4);

  /* 文字层级 */
  --text-primary:   var(--color-neutral-900);  /* #0f172a */
  --text-secondary: var(--color-neutral-600);  /* #475569 */
  --text-tertiary:  var(--color-neutral-500);  /* #64748b */
  --text-disabled:  var(--color-neutral-400);  /* #94a3b8 */
  --text-inverse:   var(--color-neutral-0);    /* #ffffff */

  /* 边框 */
  --border-default: var(--color-neutral-200);  /* #e2e8f0 */
  --border-subtle:  var(--color-neutral-100);  /* #f1f5f9 */
  --border-strong:  var(--color-neutral-300);  /* #cbd5e1 */

  /* 交互状态 */
  --interactive-hover:  rgba(15, 23, 42, 0.04);
  --interactive-active: rgba(15, 23, 42, 0.08);
  --interactive-focus:  var(--color-primary-500);
}
```

### 2.4 色彩使用规范

| 场景 | 暗色主题 | 亮色主题 |
|------|----------|----------|
| 页面背景 | `--bg-base` | `--bg-base` |
| 卡片/面板 | `--bg-surface` | `--bg-surface` |
| 弹窗/悬浮 | `--bg-elevated` | `--bg-elevated` + shadow |
| 主要文字 | `--text-primary` | `--text-primary` |
| 次要文字 | `--text-secondary` | `--text-secondary` |
| 主按钮 | `--color-primary-500` | `--color-primary-600` |
| 成功状态 | `--color-success-500` | `--color-success-600` |
| 错误状态 | `--color-error-500` | `--color-error-600` |

---

## 3. 字体系统

### 3.1 字体选择

```css
:root {
  /* ═══════════════════════════════════════════════════════════════
     Display - 标题字体
     Satoshi: 几何感、现代、有辨识度
     ═══════════════════════════════════════════════════════════════ */
  --font-display: 'Satoshi', -apple-system, BlinkMacSystemFont, sans-serif;

  /* ═══════════════════════════════════════════════════════════════
     Body - 正文字体
     General Sans: 清晰、专业、良好的中英文混排
     ═══════════════════════════════════════════════════════════════ */
  --font-body: 'General Sans', -apple-system, BlinkMacSystemFont, sans-serif;

  /* ═══════════════════════════════════════════════════════════════
     Mono - 等宽字体
     JetBrains Mono: 代码可读性优秀、数字对齐
     ═══════════════════════════════════════════════════════════════ */
  --font-mono: 'JetBrains Mono', 'SF Mono', 'Fira Code', monospace;

  /* 中文字体回退 */
  --font-chinese: 'PingFang SC', 'Microsoft YaHei', sans-serif;
}
```

### 3.2 字号系统

基于 **16px** 基准，采用 **1.25 倍率** 的模块化缩放：

```css
:root {
  /* 字号 */
  --text-xs:   0.75rem;   /* 12px - 辅助说明、徽章 */
  --text-sm:   0.875rem;  /* 14px - 次要文字、表格 */
  --text-base: 1rem;      /* 16px - 正文基准 */
  --text-lg:   1.125rem;  /* 18px - 小标题 */
  --text-xl:   1.25rem;   /* 20px - 区块标题 */
  --text-2xl:  1.5rem;    /* 24px - 页面标题 */
  --text-3xl:  1.875rem;  /* 30px - 大标题 */
  --text-4xl:  2.25rem;   /* 36px - 展示标题 */
  --text-5xl:  3rem;      /* 48px - Hero 标题 */

  /* 行高 */
  --leading-none:    1;
  --leading-tight:   1.25;
  --leading-snug:    1.375;
  --leading-normal:  1.5;
  --leading-relaxed: 1.625;
  --leading-loose:   2;

  /* 字重 */
  --font-normal:   400;
  --font-medium:   500;
  --font-semibold: 600;
  --font-bold:     700;

  /* 字间距 */
  --tracking-tighter: -0.05em;
  --tracking-tight:   -0.025em;
  --tracking-normal:  0;
  --tracking-wide:    0.025em;
  --tracking-wider:   0.05em;
}
```

### 3.3 排版预设

```css
/* 展示标题 - 用于 Hero 区域 */
.text-display {
  font-family: var(--font-display);
  font-size: var(--text-4xl);
  font-weight: var(--font-bold);
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tight);
}

/* 页面标题 - H1 */
.text-heading-1 {
  font-family: var(--font-display);
  font-size: var(--text-2xl);
  font-weight: var(--font-semibold);
  line-height: var(--leading-tight);
}

/* 区块标题 - H2 */
.text-heading-2 {
  font-family: var(--font-display);
  font-size: var(--text-xl);
  font-weight: var(--font-semibold);
  line-height: var(--leading-snug);
}

/* 小标题 - H3 */
.text-heading-3 {
  font-family: var(--font-body);
  font-size: var(--text-lg);
  font-weight: var(--font-medium);
  line-height: var(--leading-snug);
}

/* 正文 */
.text-body {
  font-family: var(--font-body);
  font-size: var(--text-base);
  font-weight: var(--font-normal);
  line-height: var(--leading-normal);
}

/* 次要文字 */
.text-body-sm {
  font-family: var(--font-body);
  font-size: var(--text-sm);
  font-weight: var(--font-normal);
  line-height: var(--leading-normal);
}

/* 代码/数据 */
.text-mono {
  font-family: var(--font-mono);
  font-size: var(--text-sm);
  font-weight: var(--font-normal);
  line-height: var(--leading-relaxed);
}

/* 标签/徽章 */
.text-caption {
  font-family: var(--font-body);
  font-size: var(--text-xs);
  font-weight: var(--font-medium);
  line-height: var(--leading-normal);
  letter-spacing: var(--tracking-wide);
  text-transform: uppercase;
}
```

---

## 4. 间距系统

### 4.1 基础间距

基于 **4px** 基准单位：

```css
:root {
  --space-0:   0;
  --space-px:  1px;
  --space-0.5: 0.125rem;  /* 2px */
  --space-1:   0.25rem;   /* 4px */
  --space-1.5: 0.375rem;  /* 6px */
  --space-2:   0.5rem;    /* 8px */
  --space-2.5: 0.625rem;  /* 10px */
  --space-3:   0.75rem;   /* 12px */
  --space-3.5: 0.875rem;  /* 14px */
  --space-4:   1rem;      /* 16px */
  --space-5:   1.25rem;   /* 20px */
  --space-6:   1.5rem;    /* 24px */
  --space-7:   1.75rem;   /* 28px */
  --space-8:   2rem;      /* 32px */
  --space-9:   2.25rem;   /* 36px */
  --space-10:  2.5rem;    /* 40px */
  --space-11:  2.75rem;   /* 44px */
  --space-12:  3rem;      /* 48px */
  --space-14:  3.5rem;    /* 56px */
  --space-16:  4rem;      /* 64px */
  --space-20:  5rem;      /* 80px */
  --space-24:  6rem;      /* 96px */
  --space-28:  7rem;      /* 112px */
  --space-32:  8rem;      /* 128px */
}
```

### 4.2 语义化间距

```css
:root {
  /* 组件内部间距 */
  --spacing-component-xs:  var(--space-1);   /* 4px - 紧凑元素间 */
  --spacing-component-sm:  var(--space-2);   /* 8px - 图标与文字间 */
  --spacing-component-md:  var(--space-3);   /* 12px - 表单元素间 */
  --spacing-component-lg:  var(--space-4);   /* 16px - 卡片内边距 */
  --spacing-component-xl:  var(--space-6);   /* 24px - 大卡片内边距 */

  /* 区块间距 */
  --spacing-section-sm:  var(--space-6);   /* 24px - 相关区块间 */
  --spacing-section-md:  var(--space-10);  /* 40px - 独立区块间 */
  --spacing-section-lg:  var(--space-16);  /* 64px - 主要区块间 */
  --spacing-section-xl:  var(--space-24);  /* 96px - 页面大区块 */

  /* 页面边距 */
  --spacing-page-x:      var(--space-6);   /* 24px - 页面水平边距 */
  --spacing-page-y:      var(--space-8);   /* 32px - 页面垂直边距 */
}
```

### 4.3 间距使用规范

| 场景 | 间距值 | 说明 |
|------|--------|------|
| 图标与文字 | `--space-2` (8px) | 水平排列 |
| 表单标签与输入框 | `--space-1.5` (6px) | 垂直排列 |
| 表单字段之间 | `--space-4` (16px) | 垂直排列 |
| 按钮内边距 | `--space-2` ~ `--space-4` | 取决于尺寸 |
| 卡片内边距 | `--space-4` ~ `--space-6` | 取决于内容 |
| 卡片之间 | `--space-4` (16px) | 网格布局 |
| 页面区块之间 | `--space-10` ~ `--space-16` | 视觉分隔 |

---

## 5. 布局系统

### 5.1 响应式断点

```css
:root {
  --breakpoint-sm:  640px;   /* 手机横屏 */
  --breakpoint-md:  768px;   /* 平板竖屏 */
  --breakpoint-lg:  1024px;  /* 平板横屏/小笔记本 */
  --breakpoint-xl:  1280px;  /* 桌面 */
  --breakpoint-2xl: 1536px;  /* 大屏桌面 */
}
```

### 5.2 三栏布局适配

```text
┌─────────────────────────────────────────────────────────────────────────────┐
│                           响应式布局策略                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  【Desktop >= 1280px】 完整三栏                                              │
│  ┌────────┬────────────────────────────┬───────────────┐                   │
│  │ NavBar │          Sidebar           │ Detail Canvas │                   │
│  │ 240px  │          flex: 1           │    400px      │                   │
│  └────────┴────────────────────────────┴───────────────┘                   │
│                                                                              │
│  【Laptop 1024px ~ 1279px】 收缩 NavBar                                     │
│  ┌──────┬──────────────────────────────┬───────────────┐                   │
│  │ Nav  │           Sidebar            │ Detail Canvas │                   │
│  │ 60px │           flex: 1            │    320px      │                   │
│  └──────┴──────────────────────────────┴───────────────┘                   │
│                                                                              │
│  【Tablet 768px ~ 1023px】 双栏 + 抽屉                                       │
│  ┌──────┬──────────────────────────────────────────────┐                   │
│  │ Nav  │                Sidebar                       │                   │
│  │ 60px │                flex: 1                       │                   │
│  └──────┴──────────────────────────────────────────────┘                   │
│          Detail Canvas → 右侧 Drawer (滑入)                                  │
│                                                                              │
│  【Mobile < 768px】 单栏 + 底部导航                                          │
│  ┌──────────────────────────────────────────────────────┐                  │
│  │                      Sidebar                         │                  │
│  │                      (全宽)                          │                  │
│  ├──────────────────────────────────────────────────────┤                  │
│  │  🏠    🤖    💬    ⚙️                                │ ← 底部 Tab Bar   │
│  └──────────────────────────────────────────────────────┘                  │
│          NavBar → 底部 Tab Bar                                              │
│          Detail Canvas → Bottom Sheet / 全屏 Modal                          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.3 容器宽度

```css
:root {
  --container-sm:  640px;
  --container-md:  768px;
  --container-lg:  1024px;
  --container-xl:  1280px;
  --container-2xl: 1536px;
}
```

### 5.4 网格系统

```css
:root {
  /* 网格列数 */
  --grid-columns: 12;

  /* 网格间距 */
  --grid-gap-sm: var(--space-3);   /* 12px */
  --grid-gap-md: var(--space-4);   /* 16px */
  --grid-gap-lg: var(--space-6);   /* 24px */
}
```

---

## 6. 阴影与层级

### 6.1 阴影系统

```css
:root {
  /* 暗色主题阴影 (更柔和) */
  --shadow-xs:  0 1px 2px 0 rgba(0, 0, 0, 0.3);
  --shadow-sm:  0 1px 3px 0 rgba(0, 0, 0, 0.4),
                0 1px 2px -1px rgba(0, 0, 0, 0.4);
  --shadow-md:  0 4px 6px -1px rgba(0, 0, 0, 0.4),
                0 2px 4px -2px rgba(0, 0, 0, 0.4);
  --shadow-lg:  0 10px 15px -3px rgba(0, 0, 0, 0.4),
                0 4px 6px -4px rgba(0, 0, 0, 0.4);
  --shadow-xl:  0 20px 25px -5px rgba(0, 0, 0, 0.4),
                0 8px 10px -6px rgba(0, 0, 0, 0.4);
  --shadow-2xl: 0 25px 50px -12px rgba(0, 0, 0, 0.5);

  /* 发光效果 (用于 focus) */
  --shadow-glow-primary: 0 0 0 3px rgba(6, 182, 212, 0.3);
  --shadow-glow-error:   0 0 0 3px rgba(239, 68, 68, 0.3);

  /* 内阴影 */
  --shadow-inner: inset 0 2px 4px 0 rgba(0, 0, 0, 0.3);
}
```

### 6.2 Z-Index 层级

```css
:root {
  --z-base:      0;
  --z-dropdown:  10;
  --z-sticky:    20;
  --z-fixed:     30;
  --z-drawer:    40;
  --z-modal:     50;
  --z-popover:   60;
  --z-tooltip:   70;
  --z-toast:     80;
  --z-max:       9999;
}
```

---

## 7. 圆角系统

```css
:root {
  --radius-none: 0;
  --radius-sm:   0.125rem;  /* 2px - 徽章、小标签 */
  --radius-md:   0.25rem;   /* 4px - 按钮、输入框 */
  --radius-lg:   0.5rem;    /* 8px - 卡片、面板 */
  --radius-xl:   0.75rem;   /* 12px - 大卡片 */
  --radius-2xl:  1rem;      /* 16px - 模态框 */
  --radius-3xl:  1.5rem;    /* 24px - 大面板 */
  --radius-full: 9999px;    /* 圆形 */
}
```

---

## 8. 动效系统

### 8.1 时长

```css
:root {
  --duration-instant:  50ms;   /* 瞬时反馈 */
  --duration-fast:     150ms;  /* 快速过渡 */
  --duration-normal:   250ms;  /* 标准过渡 */
  --duration-slow:     400ms;  /* 慢速过渡 */
  --duration-slower:   600ms;  /* 更慢过渡 */
  --duration-slowest:  1000ms; /* 最慢过渡 */
}
```

### 8.2 缓动函数

```css
:root {
  /* 标准缓动 */
  --ease-linear:    linear;
  --ease-in:        cubic-bezier(0.4, 0, 1, 1);
  --ease-out:       cubic-bezier(0, 0, 0.2, 1);
  --ease-in-out:    cubic-bezier(0.4, 0, 0.2, 1);

  /* 弹性缓动 */
  --ease-spring:    cubic-bezier(0.34, 1.56, 0.64, 1);
  --ease-bounce:    cubic-bezier(0.68, -0.55, 0.265, 1.55);

  /* 强调缓动 */
  --ease-emphasis:  cubic-bezier(0.2, 0, 0, 1);
}
```

### 8.3 动画预设

```css
/* ═══════════════════════════════════════════════════════════════
   入场动画
   ═══════════════════════════════════════════════════════════════ */

/* 淡入 */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

/* 淡入上移 */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 淡入下移 */
@keyframes fadeInDown {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 缩放入场 */
@keyframes scaleIn {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

/* 滑入 (右侧) */
@keyframes slideInRight {
  from {
    opacity: 0;
    transform: translateX(100%);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

/* 滑入 (底部) */
@keyframes slideInUp {
  from {
    transform: translateY(100%);
  }
  to {
    transform: translateY(0);
  }
}

/* ═══════════════════════════════════════════════════════════════
   加载动画
   ═══════════════════════════════════════════════════════════════ */

/* 脉冲 */
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

/* 旋转 */
@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* 闪烁光标 */
@keyframes blink {
  0%, 50% { opacity: 1; }
  51%, 100% { opacity: 0; }
}

/* 骨架屏闪烁 */
@keyframes shimmer {
  0% {
    background-position: -200% 0;
  }
  100% {
    background-position: 200% 0;
  }
}

/* ═══════════════════════════════════════════════════════════════
   状态动画
   ═══════════════════════════════════════════════════════════════ */

/* 打字机效果 */
@keyframes typing {
  from { width: 0; }
  to { width: 100%; }
}

/* 消息入场 (stagger) */
.message-enter {
  animation: fadeInUp var(--duration-normal) var(--ease-out) forwards;
}

.message-enter:nth-child(1) { animation-delay: 0ms; }
.message-enter:nth-child(2) { animation-delay: 50ms; }
.message-enter:nth-child(3) { animation-delay: 100ms; }
.message-enter:nth-child(4) { animation-delay: 150ms; }
.message-enter:nth-child(5) { animation-delay: 200ms; }
```

### 8.4 动效使用规范

| 场景 | 动画 | 时长 | 缓动 |
|------|------|------|------|
| 按钮 hover | 背景/颜色变化 | `fast` | `ease-out` |
| 按钮点击 | 轻微缩小 | `instant` | `ease-out` |
| 模态框入场 | `scaleIn` | `normal` | `ease-spring` |
| 模态框退场 | `fadeOut` | `fast` | `ease-in` |
| 抽屉滑入 | `slideInRight` | `normal` | `ease-out` |
| Toast 入场 | `fadeInUp` | `fast` | `ease-out` |
| SSE 消息入场 | `fadeInUp` (stagger) | `normal` | `ease-out` |
| 加载 Spinner | `spin` | `slowest` | `linear` |
| 骨架屏 | `shimmer` | `slowest` | `linear` |
| 思考状态 | `pulse` | `slower` | `ease-in-out` |

---

## 9. 组件规范

### 9.1 按钮 (Button)

```text
┌─────────────────────────────────────────────────────────────────┐
│                         Button 组件                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  【变体 Variants】                                               │
│  ┌──────────────┬──────────────┬──────────────┬──────────────┐ │
│  │   Primary    │  Secondary   │   Tertiary   │  Destructive │ │
│  │   ████████   │   ░░░░░░░░   │   ────────   │   ████████   │ │
│  │   主要操作    │   次要操作    │   文字按钮    │   危险操作    │ │
│  └──────────────┴──────────────┴──────────────┴──────────────┘ │
│                                                                  │
│  【尺寸 Sizes】                                                  │
│  ┌──────┬────────────┬──────────────┬────────────────────────┐ │
│  │ XS   │     SM     │      MD      │           LG           │ │
│  │ 24px │    32px    │     40px     │          48px          │ │
│  └──────┴────────────┴──────────────┴────────────────────────┘ │
│                                                                  │
│  【状态 States】                                                 │
│  Default → Hover → Active → Focus → Disabled → Loading          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

```typescript
interface ButtonProps {
  variant?: 'primary' | 'secondary' | 'tertiary' | 'destructive'
  size?: 'xs' | 'sm' | 'md' | 'lg'
  disabled?: boolean
  loading?: boolean
  leftIcon?: React.ReactNode
  rightIcon?: React.ReactNode
  children: React.ReactNode
  onClick?: () => void
}
```

```css
/* Primary Button */
.btn-primary {
  background: var(--color-primary-500);
  color: var(--color-neutral-950);
  border: none;

  &:hover:not(:disabled) {
    background: var(--color-primary-400);
  }

  &:active:not(:disabled) {
    background: var(--color-primary-600);
    transform: scale(0.98);
  }

  &:focus-visible {
    outline: none;
    box-shadow: var(--shadow-glow-primary);
  }

  &:disabled {
    background: var(--color-neutral-700);
    color: var(--color-neutral-500);
    cursor: not-allowed;
  }
}

/* Secondary Button */
.btn-secondary {
  background: transparent;
  color: var(--text-primary);
  border: 1px solid var(--border-default);

  &:hover:not(:disabled) {
    background: var(--interactive-hover);
    border-color: var(--border-strong);
  }
}

/* Button Sizes */
.btn-xs { height: 24px; padding: 0 var(--space-2); font-size: var(--text-xs); }
.btn-sm { height: 32px; padding: 0 var(--space-3); font-size: var(--text-sm); }
.btn-md { height: 40px; padding: 0 var(--space-4); font-size: var(--text-base); }
.btn-lg { height: 48px; padding: 0 var(--space-6); font-size: var(--text-lg); }
```

### 9.2 输入框 (Input)

```typescript
interface InputProps {
  type?: 'text' | 'email' | 'password' | 'search'
  size?: 'sm' | 'md' | 'lg'
  placeholder?: string
  disabled?: boolean
  error?: boolean
  errorMessage?: string
  leftIcon?: React.ReactNode
  rightIcon?: React.ReactNode
}
```

```css
.input {
  height: 40px;
  padding: 0 var(--space-3);
  background: var(--bg-surface);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-md);
  color: var(--text-primary);
  font-size: var(--text-base);
  transition: border-color var(--duration-fast) var(--ease-out),
              box-shadow var(--duration-fast) var(--ease-out);

  &::placeholder {
    color: var(--text-tertiary);
  }

  &:hover:not(:disabled) {
    border-color: var(--border-strong);
  }

  &:focus {
    outline: none;
    border-color: var(--color-primary-500);
    box-shadow: var(--shadow-glow-primary);
  }

  &:disabled {
    background: var(--bg-base);
    color: var(--text-disabled);
    cursor: not-allowed;
  }

  &[data-error="true"] {
    border-color: var(--color-error-500);

    &:focus {
      box-shadow: var(--shadow-glow-error);
    }
  }
}
```

### 9.3 卡片 (Card)

```typescript
interface CardProps {
  variant?: 'default' | 'elevated' | 'outlined'
  padding?: 'none' | 'sm' | 'md' | 'lg'
  interactive?: boolean
  children: React.ReactNode
}
```

```css
.card {
  background: var(--bg-surface);
  border-radius: var(--radius-lg);
  overflow: hidden;

  &[data-variant="default"] {
    border: 1px solid var(--border-subtle);
  }

  &[data-variant="elevated"] {
    box-shadow: var(--shadow-md);
  }

  &[data-variant="outlined"] {
    border: 1px solid var(--border-default);
    background: transparent;
  }

  &[data-interactive="true"] {
    cursor: pointer;
    transition: transform var(--duration-fast) var(--ease-out),
                box-shadow var(--duration-fast) var(--ease-out);

    &:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-lg);
    }
  }
}

.card-padding-sm { padding: var(--space-3); }
.card-padding-md { padding: var(--space-4); }
.card-padding-lg { padding: var(--space-6); }
```

### 9.4 消息气泡 (MessageBubble)

```text
┌─────────────────────────────────────────────────────────────────┐
│                      消息气泡组件                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  【用户消息】                                                    │
│                            ┌──────────────────────────────────┐ │
│                            │  帮我分析一下这份销售数据          │ │
│                            │                                  │ │
│                            │                        14:30 ✓✓ │ │
│                            └──────────────────────────────────┘ │
│                                                       ← 右对齐   │
│                                                                  │
│  【Agent 消息】                                                  │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  🤖                                                        │ │
│  │  好的，我来分析这份数据。                                    │ │
│  │                                                            │ │
│  │  从数据中可以看出...                                        │ │
│  │                                                            │ │
│  │  [点击查看完整报告 →]                                       │ │
│  │                                                   14:31    │ │
│  └────────────────────────────────────────────────────────────┘ │
│  ← 左对齐                                                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

```css
.message-bubble {
  max-width: 80%;
  padding: var(--space-3) var(--space-4);
  border-radius: var(--radius-xl);
  animation: fadeInUp var(--duration-normal) var(--ease-out) forwards;

  &[data-role="user"] {
    align-self: flex-end;
    background: var(--color-primary-600);
    color: var(--color-neutral-0);
    border-bottom-right-radius: var(--radius-sm);
  }

  &[data-role="agent"] {
    align-self: flex-start;
    background: var(--bg-elevated);
    color: var(--text-primary);
    border: 1px solid var(--border-subtle);
    border-bottom-left-radius: var(--radius-sm);
  }
}
```

### 9.5 工具调用卡片 (ToolCallCard)

```text
┌─────────────────────────────────────────────────────────────────┐
│                     工具调用卡片                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  【执行中】                                                      │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  🔧 web_search                                    ◉ 执行中 │ │
│  │  ────────────────────────────────────────────────────────  │ │
│  │  query: "2024年AI行业报告"                                  │ │
│  │  ▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░                        │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  【成功】                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  🔧 web_search                              ✓ 成功 · 1.2s │ │
│  │  ────────────────────────────────────────────────────────  │ │
│  │  找到 15 条相关结果                          [展开查看 ▼]   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  【失败】                                                        │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  🔧 code_executor                            ✗ 失败 · 0.8s │ │
│  │  ────────────────────────────────────────────────────────  │ │
│  │  ⚠️ SyntaxError: Unexpected token              [重试 ↻]    │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 9.6 计划步骤 (PlanStepper)

```text
┌─────────────────────────────────────────────────────────────────┐
│                      计划步骤组件                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  【水平模式 - 简洁】                                              │
│  ┌─────┐   ┌─────┐   ┌─────┐   ┌─────┐   ┌─────┐              │
│  │  ✓  │──▶│  ✓  │──▶│  ◉  │──▶│  ○  │──▶│  ○  │              │
│  │分析  │   │规划  │   │执行  │   │观察  │   │总结  │              │
│  └─────┘   └─────┘   └─────┘   └─────┘   └─────┘              │
│                                                                  │
│  【垂直模式 - 详细】                                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  ✓  分析用户意图                                    0.5s   │ │
│  │  │   识别关键词：销售数据、分析、趋势                        │ │
│  │  │                                                         │ │
│  │  ✓  制定执行计划                                    0.3s   │ │
│  │  │   1. 数据清洗  2. 趋势分析  3. 生成报告                  │ │
│  │  │                                                         │ │
│  │  ◉  执行数据分析                                    进行中  │ │
│  │  │   ▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░  50%                          │ │
│  │  │                                                         │ │
│  │  ○  生成分析报告                                    等待中  │ │
│  │                                                            │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 10. 加载与空状态

### 10.1 加载状态

```text
┌─────────────────────────────────────────────────────────────────┐
│                        加载状态类型                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  【Spinner - 小型加载】                                          │
│       ◠◡◠                                                       │
│     用于：按钮内、小区域                                          │
│                                                                  │
│  【Skeleton - 骨架屏】                                           │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓                                           │ │
│  │  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░                       │ │
│  │  ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░               │ │
│  │  ░░░░░░░░░░░░░░░░░░░░░░░░░░                               │ │
│  └────────────────────────────────────────────────────────────┘ │
│     用于：页面加载、列表加载                                      │
│                                                                  │
│  【思考动画 - Agent 思考中】                                      │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  💭 正在思考...                                            │ │
│  │     ● ● ●  (脉冲动画)                                      │ │
│  └────────────────────────────────────────────────────────────┘ │
│     用于：Agent 响应等待                                         │
│                                                                  │
│  【进度条 - 明确进度】                                            │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  处理中 45%                                                │ │
│  │  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░                     │ │
│  └────────────────────────────────────────────────────────────┘ │
│     用于：文件上传、长时间任务                                    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 10.2 空状态

```text
┌─────────────────────────────────────────────────────────────────┐
│                        空状态设计                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  【无数据】                                                      │
│                                                                  │
│                    ┌─────────────────┐                          │
│                    │                 │                          │
│                    │    📭           │                          │
│                    │                 │                          │
│                    └─────────────────┘                          │
│                                                                  │
│                  暂无会话记录                                     │
│              开始您的第一次对话吧                                  │
│                                                                  │
│                  [ + 新建会话 ]                                  │
│                                                                  │
│  【搜索无结果】                                                   │
│                                                                  │
│                    🔍                                            │
│              未找到 "xxx" 相关结果                                │
│                                                                  │
│              尝试：                                               │
│              • 检查拼写是否正确                                   │
│              • 使用更少的关键词                                   │
│              • 使用更通用的词汇                                   │
│                                                                  │
│  【错误状态】                                                     │
│                                                                  │
│                    ⚠️                                            │
│               加载失败                                           │
│           请检查网络连接后重试                                    │
│                                                                  │
│                  [ 重试 ]                                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 11. 图标系统

### 11.1 图标规范

- **图标库**：Lucide Icons（开源、一致性好、支持 Tree-shaking）
- **尺寸**：16px / 20px / 24px / 32px
- **描边**：1.5px（与 Lucide 默认一致）
- **颜色**：继承 `currentColor`

### 11.2 常用图标

```text
【导航】
🏠 Home          🤖 Bot           ⚙️ Settings       👤 User
📂 Folder        📊 BarChart      📝 FileText       🔍 Search

【操作】
➕ Plus          ✏️ Edit          🗑️ Trash          ↻ Refresh
📋 Copy          📥 Download      📤 Upload         ↗️ ExternalLink

【状态】
✓ Check          ✗ X              ⚠️ AlertTriangle  ℹ️ Info
◉ Circle (fill)  ○ Circle         ◐ CircleHalf     ● Dot

【Agent 相关】
💭 Thought       🔧 Tool          ▶️ Play           ⏸️ Pause
📡 Radio         🔌 Plug          🧠 Brain          ⚡ Zap
```

---

## 12. 可访问性 (A11y)

### 12.1 色彩对比度

| 组合 | 对比度 | WCAG 等级 |
|------|--------|-----------|
| `--text-primary` on `--bg-surface` | ≥ 7:1 | AAA |
| `--text-secondary` on `--bg-surface` | ≥ 4.5:1 | AA |
| `--color-primary-500` on `--bg-surface` | ≥ 4.5:1 | AA |

### 12.2 焦点管理

```css
/* 所有可交互元素必须有可见的 focus 状态 */
:focus-visible {
  outline: 2px solid var(--color-primary-500);
  outline-offset: 2px;
}

/* 鼠标用户不显示 focus 环 */
:focus:not(:focus-visible) {
  outline: none;
}
```

### 12.3 键盘导航

| 快捷键 | 功能 |
|--------|------|
| `Tab` | 焦点前进 |
| `Shift + Tab` | 焦点后退 |
| `Enter` / `Space` | 激活按钮/链接 |
| `Escape` | 关闭弹窗/取消操作 |
| `Arrow Keys` | 列表/菜单导航 |

### 12.4 ARIA 标签

```tsx
// 按钮
<button aria-label="新建会话" aria-describedby="tooltip-new-session">
  <PlusIcon aria-hidden="true" />
</button>

// 加载状态
<div role="status" aria-live="polite" aria-busy="true">
  <Spinner aria-label="加载中" />
</div>

// 错误提示
<div role="alert" aria-live="assertive">
  操作失败，请重试
</div>

// 模态框
<dialog aria-modal="true" aria-labelledby="modal-title">
  <h2 id="modal-title">确认删除</h2>
</dialog>
```

---

## 13. 实现指南

### 13.1 CSS 变量引入

```css
/* styles/tokens.css */
@import './tokens/colors.css';
@import './tokens/typography.css';
@import './tokens/spacing.css';
@import './tokens/shadows.css';
@import './tokens/motion.css';
```

### 13.2 Tailwind CSS 配置

```javascript
// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      colors: {
        primary: {
          50: 'var(--color-primary-50)',
          100: 'var(--color-primary-100)',
          // ...
          500: 'var(--color-primary-500)',
          // ...
        },
        // ...
      },
      fontFamily: {
        display: ['var(--font-display)'],
        body: ['var(--font-body)'],
        mono: ['var(--font-mono)'],
      },
      spacing: {
        // 使用 CSS 变量
      },
      animation: {
        'fade-in': 'fadeIn var(--duration-normal) var(--ease-out)',
        'fade-in-up': 'fadeInUp var(--duration-normal) var(--ease-out)',
        'spin': 'spin var(--duration-slowest) linear infinite',
        'pulse': 'pulse 2s var(--ease-in-out) infinite',
        'shimmer': 'shimmer 2s linear infinite',
      },
    },
  },
}
```

### 13.3 组件库结构

```text
packages/ui/
├── src/
│   ├── tokens/                 # 设计 Token
│   │   ├── colors.css
│   │   ├── typography.css
│   │   ├── spacing.css
│   │   ├── shadows.css
│   │   └── motion.css
│   │
│   ├── components/             # 组件
│   │   ├── Button/
│   │   │   ├── Button.tsx
│   │   │   ├── Button.styles.css
│   │   │   ├── Button.test.tsx
│   │   │   └── index.ts
│   │   ├── Input/
│   │   ├── Card/
│   │   ├── Modal/
│   │   └── ...
│   │
│   ├── compositions/           # 组合组件
│   │   ├── MessageBubble/
│   │   ├── ToolCallCard/
│   │   ├── PlanStepper/
│   │   └── ...
│   │
│   └── index.ts                # 统一导出
│
└── package.json
```

---

## 14. 版本记录

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2026-02-05 | 初始版本 |

---

## 附录：Quick Reference

### 常用色值速查

```css
/* 背景 */
--bg-base:      #020617
--bg-surface:   #0f172a
--bg-elevated:  #1e293b

/* 文字 */
--text-primary:   #f8fafc
--text-secondary: #94a3b8

/* 主色 */
--color-primary-500: #06b6d4

/* 语义色 */
--color-success-500: #10b981
--color-warning-500: #f59e0b
--color-error-500:   #ef4444
```

### 常用间距速查

```css
--space-1:  4px
--space-2:  8px
--space-3:  12px
--space-4:  16px
--space-6:  24px
--space-8:  32px
--space-12: 48px
--space-16: 64px
```

### 常用动效速查

```css
--duration-fast:   150ms
--duration-normal: 250ms
--ease-out:        cubic-bezier(0, 0, 0.2, 1)
--ease-spring:     cubic-bezier(0.34, 1.56, 0.64, 1)
```
