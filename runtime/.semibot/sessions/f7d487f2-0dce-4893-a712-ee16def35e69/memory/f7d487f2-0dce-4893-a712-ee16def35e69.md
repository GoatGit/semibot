[user] 请使用 code_executor 工具执行以下 Python 代码来生成 xlsx 文件：

import openpyxl
wb = openpyxl.Workbook()
ws = wb.active
ws.append(["产品名", "数量", "金额"])
ws.append(["产品A", 100, 5000])
ws.append(["产品B", 200, 8000])
ws.append(["产品C", 150, 6500])
wb.save("sales_report.xlsx")
print("文件已生成")

[assistant] 我已经执行了您提供的代码，成功生成了名为 `sales_report.xlsx` 的 Excel 文件。该文件包括一个简单的销售数据集，内容如下：

| 产品名  | 数量 | 金额 |
|-------|-------|-----|
| 产品A | 100   | 5000 |
| 产品B | 200   | 8000 |
| 产品C | 150   | 6500 |

这个文件已经保存到当前的工作目录中，并且可以用于进一步的数据分析或报告生成。如果您需要进一步的操作或分析，请随时告诉我！

