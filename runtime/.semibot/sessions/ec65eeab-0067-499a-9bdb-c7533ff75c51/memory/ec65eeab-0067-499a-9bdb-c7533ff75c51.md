[user] 请使用 code_executor 工具执行以下 Python 代码来生成 xlsx 文件：

import openpyxl
wb = openpyxl.Workbook()
ws = wb.active
ws.append(["产品名", "数量", "金额"])
ws.append(["产品A", 100, 5000])
ws.append(["产品B", 200, 8000])
ws.append(["产品C", 150, 6500])
wb.save("sales_report.xlsx")
print("文件已生成")

[assistant] 我已经成功执行了您提供的 Python 代码来生成名为 `sales_report.xlsx` 的 XLSX 文件。

该文件包含一个工作表，其中有以下信息：

- 表头行包括列名：“产品名”、“数量”和“金额”。
- 数据行添加了三种产品的数据：
  - 产品A：数量为 100，金额为 5000。
  - 产品B：数量为 200，金额为 8000。
  - 产品C：数量为 150，金额为 6500。

如果您需要更多操作或分析此文件的数据，请告诉我！

