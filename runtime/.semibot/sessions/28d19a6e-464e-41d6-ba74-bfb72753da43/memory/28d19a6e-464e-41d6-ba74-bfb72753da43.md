[user] 请使用 code_executor 工具执行以下 Python 代码来生成 xlsx 文件：

import openpyxl
wb = openpyxl.Workbook()
ws = wb.active
ws.append(["产品名", "数量", "金额"])
ws.append(["产品A", 100, 5000])
ws.append(["产品B", 200, 8000])
ws.append(["产品C", 150, 6500])
wb.save("sales_report.xlsx")
print("文件已生成")

[assistant] 我已经成功执行了您提供的Python代码，并生成了一个Excel文件名为 "sales_report.xlsx"。这个文件包含了一张工作表，其中列出了三个产品的销售数据，包括产品名、数量和金额。具体内容如下：

- **产品A**: 数量为100，总金额为5000。
- **产品B**: 数量为200，总金额为8000。
- **产品C**: 数量为150，总金额为6500。

如果您需要进一步处理这个文件或者有其他需求，请随时告知！

